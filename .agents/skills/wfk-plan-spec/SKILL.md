---
name: wfk-plan-spec
description: >-
  Turn a reviewed specification into a persistent task plan. Use for create-plan, plan-spec, 'crea el plan', 'planifica el trabajo', or the next stage after spec review.
---

# WFK plan-spec

Resolve the repository root as `../../..` from this skill directory.
Read `<root>/05_System/Workflows/REF - Codex Execution Policy.md` and
`<root>/05_System/Workflows/REF - Project Lifecycle.md`.
Read `<root>/skills/plan-spec/SKILL.md`; resolve its references relative to that directory.

Read the actual spec, review and project context. Read canonical planning references needed for the scope. If no ready spec/review exists, route to the missing stage. Save PL with source/review links, tasks, dependencies, status and verified counters. Continue implementation only within the user's requested scope. Prefer the shared lifecycle contract over canonical automatic team calls, redundant tier approvals or unavailable oracle dependencies.
